package ext {
/**
 * The default global window group that is available automatically.  To have more than one group of windows
 * with separate z-order stacks, create additional instances of <b class='link'>ext.WindowGroup</b> as needed.
*/
public class WindowMgr extends WindowGroup {
/**
 * @singleton
 */
}}
