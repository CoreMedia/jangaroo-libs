package {

import com.schillmania.sm2.SoundManager;

/**
 * The global soundManager object provides a collection of methods, collections, properties and event handlers.
 * Sound properties and methods can be set on a global (inherited) default, or per-sound basis.
 */
public native function get soundManager():SoundManager;

}